info:
  name: getAllArticles
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:2000/api/articles?tag=linux
  params:
    - name: tag
      value: linux
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
